/**
 * Optional host-provided datepicker locales.
 *
 * Product translations do not belong in the reusable bundle. A host can
 * expose a locale map on `globalThis.LexioAdminDatepickerLocales` before the
 * controller connects, or compile the controller source with its own locale
 * module.
 */
export default {};
