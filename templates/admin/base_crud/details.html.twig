{% extends '@LexioAdmin/admin_base.html.twig' %}

{% block title %} {{ pageTitle }} {% endblock %}

{% block heading %} {{ pageTitle }} {% endblock %}

{% block content %}
 {# Extend this template and fill that block with content... #}
{% endblock %}

{% block below_content %}
    <div class="d-flex justify-content-end mb-5 mt-3">
        <a href="{{ app.request.headers.get('referer') }}" class="btn btn-secondary">
            <i class="fa-solid fa-arrow-left me-1"></i>
            {{ 'button.back'|trans({}, 'admin') }}
        </a>
    </div>
{% endblock %}
